<%= form_for(@tweet) do |f| %>
  <% if @tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% @tweet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<form class="form-inline" role="form"> 
 <div class="form-group"> 
  <%= f.text_area :content , :class => "block custom_margin10 stable_hieght block" %>

    <%= f.submit @button_text , :class => "btn btn-large button_block btn-primary custom_margin10"%>
 </div> 
 
</form> 

 
<% end %>
